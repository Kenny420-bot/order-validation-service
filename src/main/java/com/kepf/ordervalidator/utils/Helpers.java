package com.kepf.ordervalidator.utils;

//import com.kepf.ordervalidation.soap.api.CustomerRequest;
import com.kepf.ordervalidator.model.Customer;
import com.kepf.ordervalidator.model.Orders;
import com.kepf.ordervalidator.repository.CustomerRepository;
import org.springframework.beans.factory.annotation.Autowired;



public class Helpers {
//    @Autowired
//   private  CustomerRepository customerRepository;
//
//    public static Orders newOrder(CustomerRequest request,  boolean isValid, Customer customer){
//
//            Orders order = new Orders();
//            order.setCustomer(customer);
//            order.setProduct(request.getProduct());
//            order.setPrice(request.getPrice());
//            order.setQuantity(request.getQuantity());
//            order.setSide(request.getSide());
//            order.setIs_pending(isValid);
//            order.setIs_valid(isValid);
//            return order;
//    }
}
